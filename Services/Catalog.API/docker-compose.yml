services:
  catalogdb:
    image: postgres

  catalogapi:
    build:
      context: ../../
      dockerfile: Services/Catalog.API/Dockerfile
    image: catalog-api
    depends_on:
      - catalogdb

volumes:
  catalog-db:
